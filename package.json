{
  "name": "pc-resource-manager",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"yarn server\" \"yarn client\"",
    "setup": "yarn && cd server && yarn && cd ../client && yarn",
    "teardown": "rm -rf node_modules yarn-error.log yarn.lock && cd server && rm -rf yarn.lock && cd ../client && rm -rf yarn.lock node_modules",
    "server": "cd server && yarn dev",
    "client": "cd client && yarn dev"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
